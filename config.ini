[server]
# General server settings
log_level = INFO
timeout = 30
allow_destructive = false

[binaries]
# Place your binaries under the `binaries/` folder in the project root.
# The server scans `binaries/` recursively; no explicit paths are required.
